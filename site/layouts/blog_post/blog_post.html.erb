<%= Layouts::Default.new(page: page, description: opts.fetch(:description, page.preview), **opts) do |c| %>
  <%= c.styles do %>
    <style><%= templates["blog_post.css.erb"].render(self) %></style>
  <% end %>

  <%= c.main do %>
    <time datetime="<%= page.date %>"><%= page.date.strftime("%B %-d, %Y") %></time>
    <h1><%= page.title %></h1>

    <article>
      <%= yield %>
    </article>
  <% end %>
<% end.render %>
