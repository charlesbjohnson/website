<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <title><%= title %> | CBJohnson</title>

    <%= Components::Favicon.new(emoji: "🎨").render %>

    <link rel="stylesheet" href="https://unpkg.com/modern-normalize" />
    <link rel="stylesheet" href="<%= Stylesheets::Style.uri %>" />
  </head>

  <body>
    <%= Components::Markdown.new.render do %>
      <%= templates["style_guide.md.erb"].render(self) %>
    <% end %>
  </body>
</html>
